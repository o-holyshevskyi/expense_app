const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com'], // Add this domain
  },
  i18n: {
    locales: ['en', 'uk'],
    defaultLocale: 'en'
  }
};

export default nextConfig;
